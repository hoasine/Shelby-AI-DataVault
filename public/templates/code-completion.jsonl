{"prompt": "def compute_merkle_root(data_chunks: list[bytes]) -> bytes:\n    \"\"\"Compute the Merkle root hash from a list of data chunks.\"\"\"", "completion": "\n    import hashlib\n    if not data_chunks:\n        return b''\n    hashes = [hashlib.sha256(chunk).digest() for chunk in data_chunks]\n    while len(hashes) > 1:\n        if len(hashes) % 2 == 1:\n            hashes.append(hashes[-1])\n        hashes = [\n            hashlib.sha256(hashes[i] + hashes[i+1]).digest()\n            for i in range(0, len(hashes), 2)\n        ]\n    return hashes[0]", "language": "python", "difficulty": "medium"}
{"prompt": "async function connectWallet(): Promise<string> {\n  /** Connect to user's Web3 wallet and return the connected address. */", "completion": "\n  if (typeof window.ethereum === 'undefined') {\n    throw new Error('No Web3 wallet detected');\n  }\n  const accounts = await window.ethereum.request({\n    method: 'eth_requestAccounts'\n  });\n  if (!accounts || accounts.length === 0) {\n    throw new Error('No accounts found');\n  }\n  return accounts[0];\n}", "language": "typescript", "difficulty": "easy"}
{"prompt": "def validate_signature(message: bytes, signature: bytes, public_key: bytes) -> bool:\n    \"\"\"Verify an Ed25519 signature against a message and public key.\"\"\"", "completion": "\n    from nacl.signing import VerifyKey\n    from nacl.exceptions import BadSignature\n    try:\n        verify_key = VerifyKey(public_key)\n        verify_key.verify(message, signature)\n        return True\n    except BadSignature:\n        return False\n    except Exception:\n        return False", "language": "python", "difficulty": "medium"}
{"prompt": "class TokenBucket:\n    \"\"\"Rate limiter using the token bucket algorithm.\"\"\"\n    def __init__(self, rate: float, capacity: int):", "completion": "\n        self.rate = rate  # tokens per second\n        self.capacity = capacity\n        self.tokens = capacity\n        self.last_update = time.time()\n\n    def consume(self, tokens: int = 1) -> bool:\n        now = time.time()\n        elapsed = now - self.last_update\n        self.tokens = min(self.capacity, self.tokens + elapsed * self.rate)\n        self.last_update = now\n        if self.tokens >= tokens:\n            self.tokens -= tokens\n            return True\n        return False", "language": "python", "difficulty": "medium"}
{"prompt": "function encryptWithAES(plaintext: string, key: Uint8Array): Promise<string> {\n  /** Encrypt plaintext using AES-256-GCM and return base64 ciphertext. */", "completion": "\n  const encoder = new TextEncoder();\n  const iv = crypto.getRandomValues(new Uint8Array(12));\n  const cryptoKey = await crypto.subtle.importKey(\n    'raw', key, { name: 'AES-GCM' }, false, ['encrypt']\n  );\n  const ciphertext = await crypto.subtle.encrypt(\n    { name: 'AES-GCM', iv },\n    cryptoKey,\n    encoder.encode(plaintext)\n  );\n  const combined = new Uint8Array(iv.length + ciphertext.byteLength);\n  combined.set(iv);\n  combined.set(new Uint8Array(ciphertext), iv.length);\n  return btoa(String.fromCharCode(...combined));\n}", "language": "typescript", "difficulty": "hard"}
{"prompt": "def chunk_data(data: bytes, chunk_size: int) -> list[bytes]:\n    \"\"\"Split data into fixed-size chunks with padding on the last chunk.\"\"\"", "completion": "\n    chunks = []\n    for i in range(0, len(data), chunk_size):\n        chunk = data[i:i + chunk_size]\n        if len(chunk) < chunk_size:\n            padding_len = chunk_size - len(chunk)\n            chunk += bytes([padding_len] * padding_len)\n        chunks.append(chunk)\n    if len(data) % chunk_size == 0:\n        chunks.append(bytes([chunk_size] * chunk_size))\n    return chunks", "language": "python", "difficulty": "easy"}
{"prompt": "async function fetchWithExponentialBackoff<T>(\n  fn: () => Promise<T>,\n  maxRetries: number = 5\n): Promise<T> {\n  /** Execute async function with exponential backoff on failure. */", "completion": "\n  let lastError: Error;\n  for (let attempt = 0; attempt < maxRetries; attempt++) {\n    try {\n      return await fn();\n    } catch (error) {\n      lastError = error as Error;\n      if (attempt < maxRetries - 1) {\n        const delay = Math.min(1000 * Math.pow(2, attempt), 30000);\n        const jitter = Math.random() * 1000;\n        await new Promise(r => setTimeout(r, delay + jitter));\n      }\n    }\n  }\n  throw lastError!;\n}", "language": "typescript", "difficulty": "medium"}
{"prompt": "def generate_keypair() -> tuple[bytes, bytes]:\n    \"\"\"Generate an Ed25519 keypair and return (private_key, public_key).\"\"\"", "completion": "\n    from nacl.signing import SigningKey\n    signing_key = SigningKey.generate()\n    private_key = bytes(signing_key)\n    public_key = bytes(signing_key.verify_key)\n    return private_key, public_key", "language": "python", "difficulty": "easy"}
